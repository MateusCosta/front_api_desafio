const apiProdutos = require("@/common/api").produtos;

const state = {
  produtos: {
    
  }
}

const mutations = {
 
}

const actions = {

}

const getters = {
 
}

const namespaced = true


export default {
    namespaced,
    state,
    actions,
    getters,
    mutations
}